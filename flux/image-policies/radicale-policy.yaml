apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: radicale-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: radicale
  policy:
    semver:
      range: ">=1.0.0"