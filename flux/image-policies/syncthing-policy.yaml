apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: syncthing-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: syncthing
  policy:
    semver:
      range: ">=1.0.0"
