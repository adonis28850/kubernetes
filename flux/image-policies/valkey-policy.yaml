apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: valkey-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: valkey
  policy:
    semver:
      range: ">=9.0.0"